<Window x:Class="UpdaterHelperUI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Atualizando Launcher" Height="200" Width="560"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="True">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="StatusText" Text="Preparando atualização..." FontSize="14" Margin="0,0,0,8"/>
        <ProgressBar x:Name="ProgressBar" Grid.Row="1" Height="22" Minimum="0" Maximum="100"/>
        <TextBlock x:Name="DetailsText" Grid.Row="2" TextWrapping="Wrap" Margin="0,8,0,0"/>
    </Grid>
</Window>
